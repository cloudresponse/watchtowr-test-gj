{
  "name": "watchtowr-test",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"cd client && npm run dev\" \"cd server && nest start dev --watch\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
